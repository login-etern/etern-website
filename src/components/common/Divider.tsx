/**\n * ETERN Divider Component\n * Visual separator between content sections\n */\n\nimport React from 'react'\nimport { cn } from '@/utils/cn'\n\ntype DividerVariant = 'default' | 'light' | 'dark' | 'gradient'\ntype DividerOrientation = 'horizontal' | 'vertical'\n\ninterface DividerProps extends React.HTMLAttributes<HTMLDivElement> {\n  variant?: DividerVariant\n  orientation?: DividerOrientation\n  spacing?: 'sm' | 'md' | 'lg'\n}\n\nconst variantStyles: Record<DividerVariant, string> = {\n  default: 'bg-gray-200 dark:bg-gray-800',\n  light: 'bg-gray-100 dark:bg-gray-900',\n  dark: 'bg-gray-400 dark:bg-gray-600',\n  gradient: 'bg-gradient-to-r from-transparent via-gray-300 to-transparent dark:via-gray-700',\n}\n\nconst spacingStyles: Record<'sm' | 'md' | 'lg', string> = {\n  sm: 'my-4',\n  md: 'my-8',\n  lg: 'my-12',\n}\n\nconst Divider = React.forwardRef<HTMLDivElement, DividerProps>(\n  ({\n    variant = 'default',\n    orientation = 'horizontal',\n    spacing = 'md',\n    className,\n    ...props\n  }, ref) => {\n    return (\n      <div\n        ref={ref}\n        role=\"separator\"\n        className={cn(\n          orientation === 'horizontal'\n            ? `w-full h-px ${spacingStyles[spacing]}`\n            : 'h-full w-px',\n          variantStyles[variant],\n          'transition-colors duration-200',\n          className,\n        )}\n        {...props}\n      />\n    )\n  },\n)\n\nDivider.displayName = 'Divider'\n\nexport default Divider\n