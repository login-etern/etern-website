/**\n * ETERN Section Component\n * Standardized section wrapper with consistent padding and background\n */\n\nimport React from 'react'\nimport { cn } from '@/utils/cn'\n\ntype SectionBackground = 'light' | 'dark' | 'gradient' | 'none'\ntype SectionSpacing = 'sm' | 'md' | 'lg' | 'xl'\n\ninterface SectionProps extends React.HTMLAttributes<HTMLElement> {\n  background?: SectionBackground\n  spacing?: SectionSpacing\n  id?: string\n  children: React.ReactNode\n}\n\nconst backgroundStyles: Record<SectionBackground, string> = {\n  light: 'bg-white dark:bg-gray-900',\n  dark: 'bg-gray-900 dark:bg-black',\n  gradient: 'bg-gradient-to-b from-gray-50 to-white dark:from-gray-900 dark:to-gray-950',\n  none: 'bg-transparent',\n}\n\nconst spacingStyles: Record<SectionSpacing, string> = {\n  sm: 'py-12 md:py-16',\n  md: 'py-16 md:py-24',\n  lg: 'py-20 md:py-32',\n  xl: 'py-24 md:py-40',\n}\n\nconst Section = React.forwardRef<HTMLElement, SectionProps>(\n  ({\n    background = 'light',\n    spacing = 'md',\n    className,\n    children,\n    ...props\n  }, ref) => {\n    return (\n      <section\n        ref={ref}\n        className={cn(\n          // Base styles\n          'w-full',\n          'transition-colors duration-200',\n\n          // Background\n          backgroundStyles[background],\n\n          // Spacing\n          spacingStyles[spacing],\n\n          // Custom className\n          className,\n        )}\n        {...props}\n      >\n        {children}\n      </section>\n    )\n  },\n)\n\nSection.displayName = 'Section'\n\nexport default Section\n