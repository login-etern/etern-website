/**\n * ETERN Grid Component\n * Responsive grid layout system\n */\n\nimport React from 'react'\nimport { cn } from '@/utils/cn'\n\ntype GridCols = 1 | 2 | 3 | 4 | 6 | 12\n\ninterface GridProps extends React.HTMLAttributes<HTMLDivElement> {\n  cols?: GridCols | { mobile?: GridCols; tablet?: GridCols; desktop?: GridCols }\n  gap?: 'xs' | 'sm' | 'md' | 'lg' | 'xl'\n  children: React.ReactNode\n}\n\nconst colsStyles: Record<GridCols, string> = {\n  1: 'grid-cols-1',\n  2: 'grid-cols-2',\n  3: 'grid-cols-3',\n  4: 'grid-cols-4',\n  6: 'grid-cols-6',\n  12: 'grid-cols-12',\n}\n\nconst gapStyles: Record<'xs' | 'sm' | 'md' | 'lg' | 'xl', string> = {\n  xs: 'gap-2',\n  sm: 'gap-3',\n  md: 'gap-4',\n  lg: 'gap-6',\n  xl: 'gap-8',\n}\n\nconst Grid = React.forwardRef<HTMLDivElement, GridProps>(\n  ({\n    cols = 1,\n    gap = 'md',\n    className,\n    children,\n    ...props\n  }, ref) => {\n    let colsClass = ''\n\n    if (typeof cols === 'object') {\n      const { mobile = 1, tablet = 2, desktop = 3 } = cols\n      colsClass = cn(\n        colsStyles[mobile],\n        `md:${colsStyles[tablet]}`,\n        `lg:${colsStyles[desktop]}`,\n      )\n    } else {\n      colsClass = colsStyles[cols]\n    }\n\n    return (\n      <div\n        ref={ref}\n        className={cn(\n          'grid',\n          colsClass,\n          gapStyles[gap],\n          'transition-all duration-200',\n          className,\n        )}\n        {...props}\n      >\n        {children}\n      </div>\n    )\n  },\n)\n\nGrid.displayName = 'Grid'\n\nexport default Grid\n