/**\n * ETERN Typography Components\n * Heading, Body text, and Caption components\n */\n\nimport React from 'react'\nimport { cn } from '@/utils/cn'\n\n// ============================================================================\n// Heading Component\n// ============================================================================\n\ntype HeadingLevel = 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6'\ntype HeadingSize = 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6'\n\ninterface HeadingProps extends React.HTMLAttributes<HTMLHeadingElement> {\n  level?: HeadingLevel\n  size?: HeadingSize\n  children: React.ReactNode\n  gradient?: boolean\n}\n\nconst headingSizeStyles: Record<HeadingSize, string> = {\n  h1: 'text-5xl sm:text-6xl md:text-7xl font-bold leading-tight tracking-tight',\n  h2: 'text-4xl sm:text-5xl md:text-6xl font-bold leading-tight tracking-tight',\n  h3: 'text-3xl sm:text-4xl md:text-5xl font-bold leading-tight tracking-tight',\n  h4: 'text-2xl sm:text-3xl md:text-4xl font-semibold leading-snug',\n  h5: 'text-xl sm:text-2xl md:text-3xl font-semibold leading-snug',\n  h6: 'text-lg sm:text-xl md:text-2xl font-semibold leading-snug',\n}\n\nconst Heading = React.forwardRef<HTMLHeadingElement, HeadingProps>(\n  ({\n    level = 'h1',\n    size = 'h1',\n    gradient = false,\n    className,\n    children,\n    ...props\n  }, ref) => {\n    const HeadingTag = level\n\n    return (\n      <HeadingTag\n        ref={ref as any}\n        className={cn(\n          // Base styles\n          'text-gray-900 dark:text-white',\n          'transition-colors duration-200',\n\n          // Size\n          headingSizeStyles[size],\n\n          // Gradient\n          gradient && 'bg-gradient-to-r from-blue-600 to-cyan-600 dark:from-blue-400 dark:to-cyan-400 bg-clip-text text-transparent',\n\n          // Custom className\n          className,\n        )}\n        {...props}\n      >\n        {children}\n      </HeadingTag>\n    )\n  },\n)\n\nHeading.displayName = 'Heading'\n\n// ============================================================================\n// Body Text Component\n// ============================================================================\n\ntype BodySize = 'lg' | 'base' | 'sm' | 'xs'\ntype BodyWeight = 'normal' | 'medium' | 'semibold'\n\ninterface BodyProps extends React.HTMLAttributes<HTMLParagraphElement> {\n  size?: BodySize\n  weight?: BodyWeight\n  color?: 'default' | 'secondary' | 'muted'\n  children: React.ReactNode\n}\n\nconst bodySizeStyles: Record<BodySize, string> = {\n  lg: 'text-lg leading-relaxed',\n  base: 'text-base leading-relaxed',\n  sm: 'text-sm leading-normal',\n  xs: 'text-xs leading-normal',\n}\n\nconst bodyWeightStyles: Record<BodyWeight, string> = {\n  normal: 'font-normal',\n  medium: 'font-medium',\n  semibold: 'font-semibold',\n}\n\nconst bodyColorStyles: Record<'default' | 'secondary' | 'muted', string> = {\n  default: 'text-gray-900 dark:text-white',\n  secondary: 'text-gray-700 dark:text-gray-300',\n  muted: 'text-gray-600 dark:text-gray-400',\n}\n\nconst Body = React.forwardRef<HTMLParagraphElement, BodyProps>(\n  ({\n    size = 'base',\n    weight = 'normal',\n    color = 'default',\n    className,\n    children,\n    ...props\n  }, ref) => {\n    return (\n      <p\n        ref={ref}\n        className={cn(\n          // Base styles\n          'transition-colors duration-200',\n\n          // Size\n          bodySizeStyles[size],\n\n          // Weight\n          bodyWeightStyles[weight],\n\n          // Color\n          bodyColorStyles[color],\n\n          // Custom className\n          className,\n        )}\n        {...props}\n      >\n        {children}\n      </p>\n    )\n  },\n)\n\nBody.displayName = 'Body'\n\n// ============================================================================\n// Caption Component\n// ============================================================================\n\ntype CaptionSize = 'sm' | 'xs'\n\ninterface CaptionProps extends React.HTMLAttributes<HTMLElement> {\n  size?: CaptionSize\n  uppercase?: boolean\n  children: React.ReactNode\n}\n\nconst captionSizeStyles: Record<CaptionSize, string> = {\n  sm: 'text-sm',\n  xs: 'text-xs',\n}\n\nconst Caption = React.forwardRef<HTMLElement, CaptionProps>(\n  ({\n    size = 'sm',\n    uppercase = false,\n    className,\n    children,\n    ...props\n  }, ref) => {\n    return (\n      <span\n        ref={ref}\n        className={cn(\n          // Base styles\n          'text-gray-600 dark:text-gray-400',\n          'font-medium',\n          'transition-colors duration-200',\n\n          // Size\n          captionSizeStyles[size],\n\n          // Uppercase\n          uppercase && 'uppercase tracking-wider',\n\n          // Custom className\n          className,\n        )}\n        {...props}\n      >\n        {children}\n      </span>\n    )\n  },\n)\n\nCaption.displayName = 'Caption'\n\nexport { Heading, Body, Caption }\n